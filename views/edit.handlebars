<h1 class="mb-1 restaurant-show-title">修改餐廳內容</h1>
<div class="container mb-5">
  <form action="/restaurants/{{ restaurant._id }}/edit" method="POST">
    <div class="form-row mb-3">
      <div class="form-group col-md-6">
        <label for="name">餐廳名稱</label>
        <input type="text" class="form-control" name="name" id="name" value="{{restaurant.name}}" required>
        <div class="invalid-feedback">
          請輸入餐廳名稱!
        </div>
      </div>
      <div class="form-group col-md-6">
        <label for="name_en">餐廳英文名稱</label>
        <input type="text" class="form-control" name="name_en" id="name_en" value="{{restaurant.name_en}}">
      </div>
      <div class="form-group col-6 col-md-4">
        <label for="category"><i class="fas fa-utensils pr-2"></i>類別</label>
        <input list="categories" class="form-control" name="category" id="category" value="{{restaurant.category}}" required>
        <datalist id=categories>
          {{#each category}}
          <option>{{ this }}</option>
          {{/each}}
        </datalist>
        <div class="invalid-feedback">
          請輸入餐廳類別!
        </div>
      </div>
      <div class="form-group col-6 col-md-4">
        <label for="phone"><i class="fas fa-mobile-alt pr-2"></i>電話</label>
        <input type="text" class="form-control" name="phone" id="phone" value="{{restaurant.phone}}" required>
        <div class="invalid-feedback">
          請輸入餐廳電話!
        </div>
      </div>
      <div class="form-group col-6 col-md-4">
        <label for="rating"><i class="fas fa-star fa-xs pr-2"></i>評分</label>
        <input type="number" step="0.1" class="form-control" min="0" max="5" name="rating" id="rating" value="{{restaurant.rating}}" required>
        <div class="invalid-feedback">
          請為餐廳評分!
        </div>
      </div>
      <div class="form-group col-12">
        <label for="location">地址</label>
        <input type="text" class="form-control" name="location" id="location" value="{{restaurant.location}}" required>
        <div class="invalid-feedback">
          請輸入餐廳地址!
        </div>
      </div>
      <div class="form-group col-12">
        <label for="google_map"><i class="fas fa-map-marker-alt pr-2"></i>Google Map</label>
        <input type="text" class="form-control" name="google_map" id="google_map" value="{{restaurant.google_map}}" required>
        <div class="invalid-feedback">
          請輸入餐廳google-map網址!
        </div>
      </div>
      <div class="form-group col-12">
        <label for="image">照片</label>
        <input type="text" class="form-control" name="image" id="image" placeholder="請貼上一張餐廳照片網址" value="{{restaurant.image}}">
      </div>
      <div class="form-group col-12">
        <label for="description">餐廳描述</label>
        <textarea class="form-control" name="description" id="description" placeholder="請輸入餐廳簡介">{{restaurant.description}}</textarea>
        
      </div>
    </div>
    <button type="submit" class="btn btn-primary">Save</button>
    <a href="/restaurants/{{restaurant._id}}" class="btn btn-primary">Back</a>
  </form>
</div>